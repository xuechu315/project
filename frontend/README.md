# Frontend Module

This directory contains the frontend code for the Elderly Care System.

## Structure
- Place your frontend application files here
- Can be a separate project (React, Vue, Angular, etc.)
- Or static HTML/CSS/JS files

## Notes
- The frontend is independent from the backend Maven build
- You can use any frontend framework or technology stack
- API calls should target the backend endpoints
